---
title: "Reproducible Research project week2"
author: "Koji"
date: "2021/11/5"
output: html_document
---

download zip file and unzip them
```{r}
library(ggplot2)
path <- getwd()
url <- "https://d396qusza40orc.cloudfront.net/repdata%2Fdata%2Factivity.zip"
download.file(url, file.path(path, "Activity_monitoring_data.zip"))
unzip(zipfile = "Activity_monitoring_data.zip")
```
read activity data and create sum data to sum_step
```{r}
activity <- read.csv("activity.csv")
summary(activity)
head(activity, 20)
sum_step <- aggregate(steps ~ date, data = activity, sum, na.rm = TRUE)
```
make histogram 
```{r}
hst <- qplot(steps, data = sum_step, main = "Total Number of Steps taken each day")
hst
```
